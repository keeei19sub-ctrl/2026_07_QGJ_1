# Project core
- Unity 2D game project; gameplay C# scripts are under `Assets/Scripts`, scenes/assets under `Assets`, project configuration under `ProjectSettings`.
- Unity asset/meta GUID pairs must be preserved; treat `.unity`, `.prefab`, `.asset`, and `.meta` files as Unity YAML/generated data.
- Repository-specific Git/UI rules: see `mem:conventions`.
- Toolchain and pinned versions: `mem:tech_stack`.
- Validation workflow: `mem:task_completion`; common commands: `mem:suggested_commands`.